import setuptools

# All real metadata (dependencies, version, packages, ...) lives in
# pyproject.toml. This file only exists so older tooling that doesn't
# understand pyproject.toml-only projects still works.
setuptools.setup()
